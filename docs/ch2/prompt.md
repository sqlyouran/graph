我们要从零开发一个 Java 旅行规划 Agent 课程项目。
这一章只建立工程契约，不写 Java 代码。

请创建 README.md 和 .gitignore。README 说明：
1. 输入包含出发地、目的地、日期、天数、预算、必去景点和每日节奏；
2. 航班、酒店、景点、天气必须来自可追溯数据，不能猜；
3. 终态要支持多轮修复、独立验证、有限停止和过程观测；
4. 正式候选生成必须调用百炼真实模型，测试夹具不得成为运行时回退；
5. 不做真实预订、支付和库存承诺。

先给出文件计划，确认没有生成业务源码后再执行。