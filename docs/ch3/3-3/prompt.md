请在 loop-project/web 下实现规划工作台页面，只做本节增量。

本节要解决的问题：把界面做完，四种状态全部可见，
但暂时不连后端——用假数据驱动。

当前已有能力：Vite + React + TS + Tailwind 骨架；
顶栏能显示从 /api/meta 读到的 chapter 与 model。

需要新增：
1. 三栏布局：左=请求区，中=行程区，右=过程区
   右栏本章只显示占位文案"执行过程将在第 4 章启用"
2. 请求区四个字段：目的地、天数、预算、偏好；
   提交前做基础校验（目的地非空、天数 1-7、预算 > 0）
3. 页面状态机：idle / loading / done / error 四态，
   每态在中间区有独立画面；loading 显示骨架屏与已等待秒数
4. 行程区用 react-markdown 渲染 Markdown 文本，
   右上角显示模型名与耗时角标
5. 提交时把四个字段拼成一句自然语言请求（本章不传结构化字段）
6. 用一个模块级常量 USE_MOCK 控制数据来源：
   为 true 时返回内置的假行程与 1.5 秒延迟，不发任何网络请求

明确不做：不调用后端接口、不引入状态管理库、不做多轮对话历史。
验收场景：
正常——四种状态都能手动切出来看到
失败——把假数据换成抛异常，页面进入 error 态并显示原因

先输出计划再实现。