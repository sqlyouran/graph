请基于现有代码，把模型输出从 String 改成结构化的 TripPlan。

TripPlan 要包含出发地、目的地、开始日期、天数、往返航班和每天的计划；
每天记录日期、酒店和活动；活动记录名称、类型、开始时间、结束时间和区域。
保留航班、酒店、景点的原始名称，后面要用名称回查工具快照。
价格可以保留为模型转述值用于排查，但不要设计总花费字段，总价由程序计算。

请同时完成三件事：
1. 用 Spring AI 的 entity(TripPlan.class) 接收结构化结果；
2. 结构解析失败时返回本轮问题，让它进入现有反馈循环；
3. 检查行程的出发地、目的地、日期和天数仍与原请求一致。

先列出准备修改的文件，再实现；不要改第四章的工具和事件机制。