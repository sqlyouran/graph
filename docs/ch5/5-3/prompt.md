请基于上一节的 TripPlan 和第四章的事实快照，实现四条独立检查：

C1 预算（HARD）：用航班号、酒店名、景点名回查真实价格，计算往返机票、实际住宿晚数和去重后的门票；同时检查总预算和用户填写的酒店每晚限价。查不到名称就失败。
C2 时间冲突（HARD）：同一天活动不能重叠，结束时间必须晚于开始时间；前一项结束时间等于后一项开始时间可以通过。
C3 闭馆日（HARD）：把每天日期换成星期，与景点 closedDays 比对。
C4 营业时间（HARD）：景点活动必须完整落在 openTime 到 closeTime 之间，吃饭和交通不检查。

每条都返回编号、名称、HARD/SOFT、是否通过、具体证据和修改建议；四条彼此独立，并补充边界测试。先列修改文件，再实现。