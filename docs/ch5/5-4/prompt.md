请在现有约束接口下补齐 C5-C7，并把可调整的数字放进 application.yml：

C5 路线衔接（SOFT）：按活动顺序计算跨区次数，每天最多建议2次；相邻不同区的非交通活动至少留40分钟。TRANSFER 活动表示行程明确安排了交通。
C6 必去景点（HARD）：PlanRequest 增加 mustVisit，用户点名的景点必须都出现在 ATTRACTION 活动中；“灵隐”和“灵隐寺”这类常见简称要能匹配。
C7 每日节奏（SOFT）：每天建议2-4个景点、活动总时长不超过9小时，并在11:00-14:00有 MEAL；第一天和最后一天不检查景点数量下限。

每条仍返回具体证据和修改建议。SOFT 可以写进报告，但不能单独触发下一轮。请补测试，并保持第四章 Loop 主流程不变。