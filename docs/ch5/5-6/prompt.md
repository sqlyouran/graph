请继续完善旅行规划项目。先读 TravelPlanningEngine、PlanChatService，
以及 problems 列表现有的生成方式。

现状：每轮检查后形成 problems 列表——解析失败、契约层问题、带编号的
HARD 失败建议都进这个列表；SOFT 意见只留在报告里，不进列表。
这份列表就是下一轮的返工单。这次不重写它的内容，只完成搬运与接入：

1. 第 2 轮起，模型 prompt 由四段组成：【原始需求】【上一版完整
   TripPlan】【上一轮全部问题】【修订要求】。
2. 【上一轮全部问题】整份传入 problems，一次全说，不一条一条发。
3. 【上一版完整 TripPlan】把上一轮行程原样序列化传入，让模型在旧稿
   基础上改；【修订要求】写明"保留已合格部分，逐项修复上述问题，
   并返回完整修订版，不要只返回差异或补丁"。
4. 反馈层只做搬运和分段排版，不改写建议文本——返工单质量由每条
   检查的 suggestions 保证。
5. 达到轮次上限仍未通过时，返回问题数最少的那一轮作为最好版本，
   并在响应里写清停止原因。
6. 每轮的 problems、constraintResults、feedback 记进轮次快照，
   页面能按轮查看和回放。

七条检查和建议文本都不要改。完成后告诉我：第二轮 prompt 由哪四段
组成，每一段分别来自哪行代码。